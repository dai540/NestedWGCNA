# Erode clusters to core genes

Erode clusters to core genes

## Usage

``` r
erode_to_core(clusters, dissimilarity)
```

## Arguments

- clusters:

  Integer cluster labels (`-1` for noise).

- dissimilarity:

  Gene-gene dissimilarity matrix.

## Value

Updated core cluster labels.
