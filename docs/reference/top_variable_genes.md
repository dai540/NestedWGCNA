# Select top variable genes

Select top variable genes

## Usage

``` r
top_variable_genes(x, n = 1200)
```

## Arguments

- x:

  Expression matrix (`samples x genes`).

- n:

  Number of genes to keep.

## Value

Matrix with top variable genes.
